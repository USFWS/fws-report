\usepackage{fontspec}
\usepackage{titlesec}
\usepackage{caption}
\usepackage{fancyhdr}

% Safer line spacing without setspace.sty
\linespread{1.05}

% Section formatting: ONLY # headings (\section): ALL CAPS + expanded tracking (~+1pt at 14pt)
% fontspec LetterSpace is in 1/1000 em. Approx +1pt at 14pt: 1000/14 ≈ 71
\newcommand{\FwsSectionStyle}[1]{%
  {\RobotoBold\addfontfeatures{LetterSpace=10}\MakeUppercase{#1}}%
}

\titleformat{\section}{\bfseries\fontsize{14}{16}\selectfont}{}{0pt}{\FwsSectionStyle}
\titlespacing*{\section}{0pt}{14pt}{8pt}

\titleformat{\subsection}{\bfseries\fontsize{12}{14}\selectfont}{}{0pt}{}
\titlespacing*{\subsection}{0pt}{12pt}{6pt}

\titleformat{\subsubsection}{\bfseries\fontsize{11}{13}\selectfont}{}{0pt}{}
\titlespacing*{\subsubsection}{0pt}{10pt}{4pt}

% Captions (simple, readable)
\captionsetup{
  font=small,
  labelfont=normalfont,
  textfont=normalfont,
  justification=raggedright,
  singlelinecheck=false
}
\captionsetup[table]{position=above, skip=6pt}
\captionsetup[figure]{position=below, skip=6pt}

% --- fws-report template additions (used by frontmatter.tex) ---
\usepackage{fancyhdr}
\usepackage{xurl}
\usepackage{hanging}

% ---- No paragraph indents anywhere ----
\setlength{\parindent}{0pt}

% Suppress default title/author block (we use custom frontmatter instead)
\AtBeginDocument{%
  \makeatletter
  \renewcommand{\maketitle}{}%
  \makeatother
}